import { ClosePopoverDirective } from './close-popover.directive';

describe('ClosePopoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ClosePopoverDirective();
    expect(directive).toBeTruthy();
  });
});
